/*import java.util.Scanner;

public class Matrix {
    public static void main(String[] args) {
        Scanner read = new Scanner(System.in);
        System.out.println("Gimmie the number of rows: ");
        int rows = read.nextInt();
        System.out.println("Gimmie the number of columns: ");
        int columns = read.nextInt();
        System.out.println("Gimmie the type: ");
        int type = read.nextInt();
        Operation(rows, columns, type);
    }

    public static int[][] Operation(int rows, int columns, int type) {
        int matrix[][] = new int[rows][columns];
        {
            if (type == 1) {
                diagonalM(matrix);
            }

        }


    }

    public static int[][] diagonalM(int matrix[][]) {
        for (int i = 1; i < matrix.length; i++) {

        }
    }
}
*/